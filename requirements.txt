# Install everything needed to run the notebooks from the committed data:
#   pip install -e .
# Add satellite re-extraction dependencies (uhi_pipe / notebook 01):
#   pip install -e ".[extract]"
-e .
